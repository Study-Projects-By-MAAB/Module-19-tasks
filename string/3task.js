// Check whether a string contains all the vowels a, e, i, o, u

let string = "Check whether a string contains all the vowels a, e, i, o, u"

if (string.includes('a' && 'e' && 'i' && 'o' && 'u')) {
    console.log("yes it's have");
}
else {
    console.log("no it's not!");
}